# План реализации: нейросетевое управление посадкой и пробегом (ИСМПУ)

Шифр НИР: **Интеграл-КБО-МС-ГосНИИАС-ИСМПУ-2026**. Срок: **28.07.2026**, целевой УГТ 3.

Документ описывает переход от текущего прототипа (Jupyter [`main.ipynb`](main.ipynb)) к
модульной системе гибридного управления: классический PID-контур + нейросетевая подстройка
коэффициентов (PIDNN-actor, PPO), защитный контур (Shield) и, опционально в будущем,
физически-информированный обсервер (PINN/PD-PINN). Архитектура — по диаграмме
[`PIDNN.mmd`](PIDNN.mmd), требования — по ТЗ [`ТЗ_Интеграл-КБО-МС_ИСМПУ_итог_ф.pdf`](ТЗ_Интеграл-КБО-МС_ИСМПУ_итог_ф.pdf).

---

## 1. Ключевые принципы

1. **Классический контур — это plant, а не конкурент.** Нейросеть не заменяет PID, а генерирует
   *мультипликативные поправки* к его коэффициентам и веса влияния каналов. При тождественных
   поправках (α = 1, веса = базовые) система обязана бит-в-бит воспроизводить классическое поведение.
   Это инвариант, проверяемый тестом на каждом этапе.
2. **Разделение по динамике, а не по органам.** Два канала: **продольный** (реверс + тормоза, симметрично)
   и **латеральный** (руление + дифференциальное торможение + ассиметричная тяга). Управление — распределением степени влияния
   каналов. Отказ (например NWS) → повышение веса латерального канала, а не переобучение с нуля.
3. **Одна абстракция среды — два бэкенда.** Обучение идёт на X-Plane 12, поставка (ТЗ 5.1.4) — `.exe`
   поверх согласованного ПИВ на стенд Заказчика ([`ics_connector.py`](ics_connector.py)). Оба скрыты за
   единым интерфейсом `SimInterface`, чтобы актор/шилд/контур не зависели от транспорта.
4. **Безопасность вне сети.** Shield — детерминированная функция, не обучается, всегда активна, имеет
   fallback на чистый классический контур. Нейросеть не может выдать команду, минуя Shield.
5. **Обсервер — опция с зарезервированными швами.** PINN не строим сейчас, но заранее резервируем слоты
   в Observation Space, тип `ObserverEstimate` и слагаемое `L_phys`, чтобы включение было неинвазивным.
6. **Детерминизм при поставке.** Замороженные веса, фиксированная нормализация, greedy-инференс,
   без обучения в рантайме (диаграмма, блок 7).

---

## 2. Соответствие требованиям ТЗ

Требования ТЗ становятся (а) целевыми порогами reward-шейпинга, (б) жёсткими гейтами приёмочного
тестирования (Этап 5). Таблица «требование → метрика → где реализуется»:

| Пункт ТЗ | Требование | Метрика / гейт | Реализация |
|---|---|---|---|
| 5.1.1.2 | Ось ВС ↔ ось ВПП ±5 м на H=30 м; касание в первой трети / ≤900 м | XTE на пороге, точка касания | Латеральный канал, guidance |
| 5.1.2.4 | Асимметрия тяги → компенсация, курс ±5° от оси | max\|heading_err\| | Латеральный + продольный (асимм. реверс) |
| 5.1.3.1 | Пробег: удержание оси **±3 м**; руление на прямой **±1 м** | max\|XTE\| по фазам | Латеральный канал |
| 5.1.3.1 | Диагностика: μ ВПП, боковой ветер, риск аквапланирования | оценки обсервера / эвристики | **PINN-обсервер** (опция) + эвристики |
| 5.1.3.2 | Отказ NWS → перераспределение на дифф. торможение + асимм. тягу; ось **±5 м** до полной остановки | max\|XTE\| при NWS-fail | Веса каналов (латеральный↑) |
| 5.1.3.3 | Отказ тяги/реверса → компенсация рыскания, скоростной профиль, курс **±5° до <30 узлов** | heading_err до v<30 kts | Оба канала + Shield runtime |
| 5.1.1.4 | Нейромодули: адаптация в реальном времени, устойчивость/плавность, обучаемость на данных Заказчика | L_smooth, стабильность | PIDNN + PPO + L_smooth |
| 5.1.4 | `.exe`, сетевой обмен по ПИВ со стендом | интеграционный тест на стенде | Deployment + ICS backend |
| 5.1.5 | Статобработка, разбор аномалий/деградаций, интерпретация вклада нейромодулей, обобщающая способность; GUI сравнения с классикой | отчёт + сравнительный GUI | Eval harness + GUI |

Пороговые числа выносятся в единый конфиг `config/requirements.py`, чтобы reward и eval ссылались на
один источник истины.

---

## 3. Целевая структура проекта

Имя пакета — `ismpu/` (по шифру ИСМПУ; согласовано). Доменное разбиение:

```
ismpu/
  io/                      # ТРАНСПОРТ (только ввод-вывод, без логики управления)
    xplane_connector.py    #  ← XPlaneConnectX.py (без изменений поведения)
    ics_connector.py       #  ← ics_connector.py
    datarefs.py            #  именованные константы DREF + справочник (из DataRefs.txt)
    sim_interface.py       #  абстрактный SimInterface + XPlaneBackend / ICSBackend
  control/                 # КЛАССИЧЕСКИЙ КОНТУР (plant-agnostic, без сети)
    pid.py                 #  ← PIDController
    runway_tracker.py      #  ← RunwayTracker (геодезия, guidance, lookahead)
    trajectory.py          #  ← ReferenceTrajectory, VelocityLaw
    channels.py            #  ← LongitudinalChannel, LateralChannel
    system.py              #  ← ControllingSystem, ControlsState
    failures.py            #  ← FailureMode / FailureManager / FailureState
  config/
    runway.py              #  константы ВПП (UUEE 06R и др.)
    constants.py           #  DT, FREQ, скорости
    scenarios.py           #  пресеты PID-коэффициентов по сценариям (из ячеек ноутбука)
    requirements.py        #  пороги ТЗ (±3 м, ±5°, <30 узлов, ...)
  utils/
    converts.py            #  ← Converts
  envs/                    # RL-СРЕДА
    observation.py         #  сборка и нормализация вектора состояния
    action.py              #  пространство действий (мультипл. поправки + веса каналов)
    reward.py              #  покомпонентный reward / термы критика
    scenario_generator.py  #  domain randomization: погода, отказы, шум, μ
    rollout_env.py         #  Gymnasium-совместимая среда (оборачивает SimInterface + control)
  agent/                   # НЕЙРОСЕТЬ
    pidnn_actor.py         #  PIDNN multi-head (Heading / Brake / Reverse / [Flap])
    critic.py              #  V(s)
    ppo.py                 #  PPO-тренер + многокомпонентный loss
    shield.py              #  3-уровневый Shield + fallback
    observer.py            #  (ОПЦИЯ) PINN/PD-PINN — сейчас интерфейс-заглушка
    normalization.py       #  фиксированные константы нормализации (общие train/deploy)
  runtime/
    train.py               #  цикл обучения PPO
    evaluate.py            #  приёмочный прогон по батарее сценариев + статистика
    deploy.py              #  детерминированный рантайм на замороженных весах
    loop.py                #  20 Гц управляющий цикл (замена run-ячейки ноутбука)
  gui/                     # ГРАФИЧЕСКИЙ ИНТЕРФЕЙС (ТЗ 5.1.5)
    ...                    #  сравнение классика vs нейро, телеметрия, вклад модулей
tests/
  test_control_parity.py   #  инвариант: identity-поправки == классика
  test_shield.py
  test_observation.py
  ...
```

Файлы `main.py` и `env.py` — устаревшие эксперименты, при реструктуризации удаляются/архивируются.

---

## 4. Этап 0 — Реструктуризация (без изменения поведения) — ✅ ВЫПОЛНЕНО

**Цель:** вынести логику из ноутбука в пакет `ismpu/`, ничего не сломав.

**Итог:** пакет `ismpu/` создан (io / control / config / utils / runtime); `main.ipynb` стал тонким
фронтом; добавлены `pyproject.toml` / `requirements.txt` / `.gitignore` / корневой `conftest.py` и
`tests/test_control_parity.py` (13 симулятор-независимых тестов — все проходят). Намеренные отличия от
ноутбука, не меняющие траекторию: (1) отладочный вывод `PIDController` переведён с `cprint` на
`logging.debug`; (2) `ControllingSystem.setup` подключает каналы к `self.xpc` вместо модульной глобали.
Черновые пресеты реверса помечены `draft=True`. Полный паритет траектории с ноутбуком — ручная проверка
на X-Plane.

- Перенести классы 1:1 в модули из §3, сохранив русские комментарии и имена.
- Runway-константы и пресеты PID из ячеек ноутбука → `config/`.
- Оформить зависимости: `pyproject.toml` / `requirements.txt` (numpy, pandas, termcolor, torch,
  gymnasium, matplotlib; зафиксировать версии из `.venv`).
- Заменить `cprint`-отладку внутри `PIDController.compute` на логгер с уровнями (в горячем цикле по
  умолчанию молчит) — иначе на 20 Гц × 5 регуляторов консоль забивается.
- `main.ipynb` оставить как тонкий демонстрационный фронт: импорт из `ismpu`, запуск `runtime/loop.py`.
- **Критерий готовности:** прогон нового `runtime/loop.py` на дефолтном сценарии даёт ту же траекторию,
  что и ноутбук (тест паритета `test_control_parity.py`).

---

## 5. Observation Space (полный вектор состояния)

Собирается в `envs/observation.py` из телеметрии (`SimInterface`), состояния классического контура и
(в будущем) оценок обсервера. **Все компоненты нормируются** фиксированными константами из
`agent/normalization.py` (единые для обучения и поставки — иначе детерминизм ломается).

Регуляторов **5**: `steer`, `brake_l`, `brake_r`, `rev_l`, `rev_r` (в целевой архитектуре mermaid есть
6-й — `flaps`; добавляется опционально, см. §10).

| Блок | Признак | Ед. | Диапазон (норм.) | Источник |
|---|---|---|---|---|
| **Геометрия** | cross_track_error | м | ±30 → /30 | tracker |
| | heading_error | град | ±45 → /45 | tracker.guidance |
| | distance_to_end | м | 0..L → /L | tracker |
| | lookahead | м | 10..120 → /120 | tracker |
| **Скорость** | ground_speed | м/с | 0..110 → /110 | телеметрия |
| | reference_speed | м/с | 0..110 → /110 | trajectory |
| | speed_error | м/с | ±50 → /50 | вычисл. |
| | longitudinal_acceleration | м/с² | ±5 → /5 | конечная разность v |
| **Управление** (последнее применённое) | brake_l, brake_r | — | [0,1] | ControlsState |
| | reverse_l, reverse_r | — | [0,1] | ControlsState |
| | rudder_cmd | — | [-1,1] | ControlsState |
| **PID × 5 регуляторов** | Kp, Ki, Kd | — | log-scale, норм. по базе | PID (динам.) |
| | integral | — | ±anti_windup → /aw | PID (динам.) |
| | derivative (filtered) | — | норм. по масштабу | PID (динам.) |
| | output | — | [min_out,max_out]→[-1,1] | PID (динам.) |
| | min_out, max_out, integral_decay, der_filter_tf | — | константы сценария | PID (статич.*) |
| **Отказы** | reverse_l_fault, reverse_r_fault, nws_fault | флаг | {0,1} | FailureState / сценарий |
| **Погода** | wind_along, wind_cross | м/с | ±20 → /20 | телеметрия/сценарий |
| | runway_friction μ | — | 0..1 | сценарий (или обсервер) |
| | rain / precip | — | 0..1 | сценарий |
| | visibility | м | 0..10000 → лог | сценарий |
| **(Опция) Обсервер** | μ̂, ŵ_cross, brake_eff, nws_eff, τ_delay | — | норм. | PINN (§10) |

\* Статические PID-параметры (`min_out/max_out/integral_decay/der_filter_tf`) в текущей схеме актор **не
меняет** (он модулирует только Kp/Ki/Kd) — они постоянны в пределах сценария. Их включение в obs
избыточно; оставляем как **опциональный флаг** `include_static_pid_features` (нужно, только если позже
Shield/актор начнут адаптировать и границы). По умолчанию исключаем ради снижения размерности.

**История и рекуррентность.** Диаграмма предусматривает shared encoder GRU/LSTM и temporal encoder
обсервера над окном `X_t … X_{t-n}`. Поэтому:
- `observation.py` возвращает **один кадр**; окно собирает кольцевой буфер в `rollout_env.py`
  (`obs_history`, глубина `n` в конфиге).
- Актор с рекуррентным encoder читает последовательность; для MLP-варианта — конкатенацию кадров.

**Нормализация — единый контракт.** `normalization.py` хранит `mean/scale` (или физические пределы) как
константы, сериализуемые вместе с весами при экспорте (§11). Никаких «плавающих» статистик на инференсе.

---

## 6. Action Space (действие актора)

Актор выдаёт **мультипликативные поправки**, а не абсолютные величины (философия §1).

1. **Поправки к коэффициентам PID.** Для каждого регулятора — `(α_p, α_i, α_d)`, центрированы на 1.0,
   применяются как `Kp_eff = Kp_base · α_p` (аналогично Ki, Kd). Головы по диаграмме: Heading (steer),
   Brake (L+R), Reverse (L+R), [Flap]. Итого `5×3 = 15` (или `6×3 = 18` с закрылками) поправок.
2. **Веса влияния каналов.** Скаляры (или мягкое распределение) `w_lon`, `w_lat` — степень вклада выходов
   продольного и латерального каналов. Именно ими достигается перераспределение при отказах (NWS →
   `w_lat↑`). Реализуются как множители к выходам каналов перед микшированием в `ControlsState`.
3. Выход головы — параметры распределения политики (mean, log_std) для PPO; действие сэмплируется при
   обучении, берётся mean (greedy) при поставке.

Границы поправок (например α ∈ [0.5, 1.5]) — это **первый уровень Shield** (§8), а не свойство сети.

---

## 7. Абстракция симулятора (`envs/sim_interface.py`) — реализовано

Единый интерфейс, за которым скрыт транспорт. Размещён в `envs/` (не `io/`): зависит от
`WeatherState`/`FailureMode`/`Scenario`, поэтому находится выше слоя транспорта. `reset`/`step`
возвращают **сырую** `Telemetry` (нормализованный Observation собирается в Этапе 2, §5).

```
class SimInterface(ABC):
    def reset(self, scenario: Scenario) -> Telemetry
    def step(self, command: ControlsState) -> Telemetry        # шаг 20 Гц
    def read_telemetry(self) -> Telemetry
    def apply_weather(self, w: WeatherState) -> None            # ветер, μ, дождь, видимость
    def inject_failure(self, f: FailureMode) -> None
    def clear_failures(self) -> None
    def teleport_touchdown(self, setup: TouchdownSetup) -> None
    def pause(self, flag: bool) -> None
    def update(self, distance_m: float) -> None                 # переменное сцепление и т.п.
    def close(self) -> None
```

- **`XPlaneBackend`** (обучение): обёртка над `XPlaneConnectX`; асинхронная телеметрия через
  `subscribeDREFs`; телепорт/пауза для быстрого сброса эпизода; запись погоды/μ через DREF (какие именно
  writable-DREF-ы — уточнить по [`DataRefs.txt`](DataRefs.txt), раздел `sim/weather/…`; вынести в отдельную
  research-задачу Этапа 1).
- **`ICSBackend`** (поставка): обёртка над `ICSBenchConnector`; маппинг `ICSInputs → Observation` и
  `ControlsState → ICSOutputs`. Управление погодой/отказами на стенде задаёт Заказчик — на этом бэкенде
  `apply_weather`/`inject_failure` no-op или диагностика читается из `ICSInputs.Fault*`/`RunwayCondition`.

Актор, Shield, критик, env зависят только от `SimInterface`. Это выполняет ТЗ 5.1.4 (перенос на стенд без
переписывания логики).

---

## 8. Генератор сценариев (`envs/scenario_generator.py`)

Диаграмма, блок 0. Сэмплирует ситуации из **всего** пространства признаков — нужны и штатные, и «ужасные»
(для робастности PPO). Оси рандомизации:

- **ВПП/сцепление:** сухая / мокрая / лёд (μ), переменное μ по длине.
- **Ветер:** постоянный боковой, порывы (случайный процесс).
- **Отказы:** NWS, отказ реверса Л/П, асимметрия тяги, некорректная конфигурация шасси, комбинации.
- **Датчики:** гауссов шум, смещения, выпадения (для робастности и OOD-детектора Shield).
- **Начальные условия:** скорость касания, точка касания, смещение от оси, курс.
- **Domain randomization:** совместная выборка + учебный план (curriculum: от штатных к тяжёлым).

Каждый сценарий — сериализуемый `Scenario` (для воспроизводимости и приёмочной батареи Этапа 5).
Пресеты классических PID из ячеек ноутбука (`# default`, `# left reverse fail`, `# NWS fault`, …)
переносятся в `config/scenarios.py` и служат как (а) базой для поправок, (б) классическим бейзлайном для
сравнения в GUI.

---

## 9. Shield — защитный контур (`agent/shield.py`)

Детерминированная функция между актором и контуром. Три уровня + fallback (диаграмма, блок 4).

1. **Уровень 1 — поправки.** Clip слишком больших `α` и весов каналов к допустимым границам.
2. **Уровень 2 — коэффициенты PID.** После применения поправок проверяем `Kp/Ki/Kd`: hard bounds,
   rate limit (ограничение скорости изменения между тактами), консистентность знаков и диапазонов, OOD
   (выход состояния/коэффициентов за обучающее распределение).
3. **Уровень 3 — Runtime Safety.** Поведенческие проверки: слишком быстрое изменение коэффициента,
   чрезмерно агрессивные тормоза, реверс на низкой скорости (<60 узлов, ср. текущую логику канала),
   срыв по курсу и т.п.
4. **Projection to Safe Set / Fallback.** При нарушении — проекция в безопасное множество; при OOD/грубом
   нарушении — переключение на **классический PID** (или автопилот).

Связь с loss: активация любого уровня → штраф в `L_shield`; резкие скачки коэффициентов → в `L_smooth`.
Shield реализуется и тестируется **до** актора — как чистая функция над `(поправки, coeffs, runtime_state)`,
с юнит-тестами на каждое правило.

---

## 10. PIDNN Actor + Critic (`agent/`)

- **Actor (PIDNN multi-head):** shared encoder (GRU/LSTM или MLP над окном) → shared dense → головы
  Heading / Brake / Reverse / [Flap]. Каждая голова выдаёт `(α_p, α_i, α_d)` для своих регуляторов;
  отдельный выход — веса каналов `w_lon/w_lat`. Малый размер (реалтайм <50 мс на такт — тривиально).
- **Critic:** `V(s)` над тем же состоянием (обычно отдельная голова/сеть).
- **Multi-head мотивация (из описания):** отказ → правка веса канала, а не переобучение; симметричное
  общее торможение при почти независимом рулении; критик проще настраивать по каналам; PPO обучается легче.

---

## 11. PPO + многокомпонентный loss (`agent/ppo.py`, `runtime/train.py`)

Диаграмма, блок 6. `L = L_ppo + λ_s·L_smooth + λ_p·L_phys + λ_sh·L_shield`.

- **L_ppo** — стандартный clipped surrogate + GAE-advantage от критика.
- **L_smooth** — штраф за рывки управления и резкие скачки коэффициентов PID (плавность gain).
- **L_phys** — *опционально*, включается вместе с обсервером (§12): штраф за нефизичный residual.
  До появления обсервера `λ_p = 0`, но слагаемое и хук в loss уже присутствуют.
- **L_shield** — штраф за вмешательство Shield (по уровням).
- Оптимизатор — AdamW.
- **Покомпонентный критик / reward** (описание, разд. V): отдельно оцениваются боковое отклонение,
  ошибка скорости, рывки, активация Shield, срыв по курсу, нестабильность. Reward-термы и их веса — в
  `envs/reward.py`, пороги — из `config/requirements.py`.
- Логирование: TensorBoard/CSV по каждому терму, манифест запуска (seed, сценарии, гиперпараметры).

**Выбор библиотеки — согласовано: компактный собственный PPO на PyTorch** (стиль CleanRL) — из-за
нестандартного действия (мультипликативные поправки + веса каналов), multi-head политики и
кастомного многокомпонентного loss с хуками Shield/Phys. Stable-Baselines3 остаётся запасным вариантом
через custom policy, но кастомизация loss в нём тяжелее.

---

## 12. (Опция) PINN / PD-PINN Observer (`agent/observer.py`)

Пока **не реализуем**, но проектируем швы. Диаграмма, блок 2.

- **Назначение:** восстановление скрытых параметров динамики → выполнение диагностических требований ТЗ
  5.1.3 (μ, боковой ветер, аквапланирование) и обогащение состояния актора:
  - μ (коэффициент сцепления ВПП);
  - эффективное торможение → коэффициент эффективности тормозов (из фактического замедления и команды);
  - степень деградации эффективности руления (NWS) — по фактическому повороту при заданной команде;
  - боковая компонента ветра;
  - задержка/инерционность динамики (τ) — с учётом инерции ЛА, люфта, проскальзывания шин, гидравлики.
- **Обучение отдельно:** классический System Identification при взаимодействии с симулятором, возможно без
  PPO. Архитектура: история состояний → Temporal Encoder (GRU/LSTM) → Dense → Physics Head → `μ̂, ŵ, brake_eff, nws_eff, τ̂`.
- **Точки интеграции, резервируемые сейчас:**
  1. Слоты обсервера в Observation Space (§5, помечены «Опция») — заранее в схеме вектора.
  2. Тип `ObserverEstimate` и метод `SimInterface`/env, возвращающий оценки (сначала заглушка нулями/
     ground-truth из сценария).
  3. Слагаемое `L_phys` и его хук в loss (§11), `λ_p = 0` до включения.
- За нефизичное поведение обсервер начисляет штраф в `L_phys`.

---

## 13. Оценка, тестирование, GUI (`runtime/evaluate.py`, `gui/`)

ТЗ 5.1.5. Приёмка — не «сеть обучилась», а **прохождение гейтов ТЗ**.

- **Приёмочная батарея сценариев:** фиксированный набор `Scenario` (штатные + все отказы + погода),
  seeded, воспроизводимый. По каждому — метрики из §2 против порогов `config/requirements.py`.
- **Статобработка и разбор аномалий/деградаций** по сценариям; интерпретация вклада нейромодулей
  (сравнение с отключённой сетью = чистая классика), проверка обобщающей способности (hold-out сценарии).
- **GUI сравнения (ТЗ 5.1.5.3):** визуализация траектории, XTE/heading/скорости, вклада каналов и Shield,
  side-by-side «классика vs нейро». Выбор стека GUI — на Этапе 5 (matplotlib/веб-дашборд).
- Тесты-инварианты на всех этапах: паритет с классикой, корректность Shield, диапазоны нормализации.

---

## 14. Deployment (`runtime/deploy.py`)

Диаграмма, блок 7; ТЗ 5.1.4.

- Заморозка весов PIDNN (+ PINN при наличии); экспорт весов **вместе с константами нормализации** одним
  артефактом.
- Детерминированный рантайл: greedy-инференс, без обучения, Shield активна, fallback доступен.
- Транспорт — `ICSBackend` (ПИВ на стенд, порт/протокол по `ics_connector.py`).
- Упаковка в `.exe` (PyInstaller/аналог) с сетевым обменом по согласованному ПИВ.
- Требования к оформлению (ГОСТ 19.101-2024, спецификация, описание, исходный/исполняемый код,
  руководство оператора, паспорт) — учитываются на этом этапе.

---

## 15. Дорожная карта и план по этапам

Сводная таблица (порядок и зависимости), далее — детальный состав работ по каждому этапу.
Этап 0 полностью описан в §4; здесь — Этапы 1–7. Каждый этап оформлен единообразно:
**Цель / Состав работ / Критерий готовности / Артефакты**. Домены (§5–§14) дают проектную
детализацию, этот раздел — упорядоченный список задач и то, что предъявляется.

| Этап | Содержание | Зависит от | Критерий готовности |
|---|---|---|---|
| **0** | Реструктуризация ноутбука → `ismpu/` | — | Паритет траектории с ноутбуком |
| **1** | `SimInterface` + X-Plane/ICS бэкенды, инъекция погоды/отказов, генератор сценариев | 0 | Сброс эпизода + рандомизация работают |
| **2** | Observation/Action Space, нормализация, `rollout_env` (Gymnasium) | 1 | Env с identity-поправками == классика |
| **3** | Shield (3 уровня + fallback) как чистая функция | 2 | Юнит-тесты на все правила |
| **4** | PIDNN actor + critic + PPO + многокомпонентный loss; обучение с DR | 2, 3 | Сеть обучается, гейты штатного сценария |
| **5** | Eval harness, статистика, гейты ТЗ, GUI сравнения | 4 | Прохождение приёмочной батареи |
| **6** | Deployment: заморозка, детерминизм, ICS, `.exe` по ПИВ | 4, 5 | Интеграция на стенде Заказчика |
| **7** *(опция, параллельно с 4–5)* | PINN/PD-PINN обсервер, включение `L_phys`, диагностика ТЗ 5.1.3 | 2 (швы) | Оценки μ/ветра/эффективности валидны |

Швы обсервера (Этап 7) закладываются уже на Этапе 2, чтобы включение не требовало переработки среды и loss.

### Этап 1 — Абстракция среды и генератор сценариев — ✅ ВЫПОЛНЕНО (в коде)

**Цель:** отвязать контур от транспорта и научиться сбрасывать/рандомизировать эпизод, не трогая логику
управления. См. §7, §8.

**Итог:**
- ✅ `envs/sim_interface.py`: `SimInterface` (ABC) — `reset / step / read_telemetry / apply_weather /
  inject_failure / clear_failures / teleport_touchdown / pause / update / close`; `Telemetry` (сырая
  телеметрия в СИ). Размещён в `envs/` (не в `io/`), т.к. зависит от `WeatherState`/`FailureMode`/`Scenario`
  — держать в `io/` нарушило бы «io = только транспорт».
- ✅ `XPlaneBackend`: обёртка над `XPlaneConnectX` — расширенная подписка телеметрии, быстрый лёгкий
  телепорт со смещением от оси/курса (без 30-с reload из `runtime/setup.py`), погода через `WeatherManager`,
  инъекция отказов реальными failure-DataRef'ами двигателей/реверса (`rel_engfai{N}` / `rel_revers{N}`,
  enum 6=отказ). NWS/деградация тяги/шасси у X-Plane датарефа не имеют → помечаются активными, эффект даёт
  деградация команд в контуре.
- ✅ `ICSBackend`: обёртка над `ICSBenchConnector` — `ICSInputs → Telemetry`, `ControlsState → ICSOutputs`
  (маппинг предварительный, §17 п.2); погоду/отказы/телепорт задаёт стенд → no-op.
- ✅ **Управление погодой** (сделано на прошлом шаге): `envs/weather.py` (`WeatherManager`, `WeatherState`,
  боковой ветер, `FrictionProfile`, `WEATHER_PRESETS`); `SimInterface.apply_weather`/`update` — обёртки.
- ✅ `envs/scenario.py`: сериализуемые `Scenario` / `TouchdownSetup` / `SensorNoise` (`to_dict`/`from_dict`).
- ✅ `envs/scenario_generator.py`: `ScenarioGenerator` — domain randomization по всем осям (μ/состояние ВПП,
  боковой ветер, порывы/турбулентность, дождь/видимость/температура, отказы, шум, НУ) с curriculum-параметром
  `difficulty ∈ [0,1]`; детерминизм по seed; `battery()` — фиксированный приёмочный набор.

**Критерий готовности:** `reset(scenario)` телепортирует и конфигурирует ЛА; рандомизация выдаёт валидные
сериализуемые эпизоды; последовательность генератора воспроизводима по seed. Полный прогон классического
loop через `SimInterface` на живом X-Plane — ручная проверка (как паритет Этапа 0).

**Артефакты:** `envs/{sim_interface,scenario,scenario_generator}.py`, расширенный `io/datarefs.py`
(телеметрия + failure-DataRef'ы); `tests/test_sim_interface.py` (17 тестов: бэкенды через mock, инъекция
отказов, сериализация `Scenario`, детерминизм и валидность генератора). Всего в наборе — 44 теста.

### Этап 2 — Observation/Action Space + RL-среда

**Цель:** Gymnasium-среда поверх `SimInterface` + классического контура; выполнить инвариант «identity ==
классика». См. §5, §6.

**Состав работ:**
- `envs/observation.py`: сборка вектора состояния (геометрия / скорость / последнее управление / PID×5 /
  отказы / погода) + **зарезервированные слоты обсервера** (нулями); возвращает один кадр.
- `agent/normalization.py`: фиксированные `mean/scale` (физические пределы), сериализуемые вместе с весами;
  единый контракт train↔deploy.
- `envs/action.py`: пространство действий — поправки `(α_p, α_i, α_d)` на каждый регулятор + веса каналов
  `w_lon/w_lat`; применение поправок к копии базовых `Kp/Ki/Kd`; веса как множители к выходам каналов перед
  `clamp_all`.
- `envs/rollout_env.py`: Gymnasium `Env` (`reset/step`); кольцевой буфер истории `obs` (глубина `n`);
  `envs/reward.py` — покомпонентный reward, пороги из `config/requirements.py` (§11).
- Заложить швы обсервера: тип `ObserverEstimate` (заглушка нулями/ground-truth), слоты в obs (§12).

**Критерий готовности:** `env.step` с тождественными поправками (α=1, веса базовые) бит-в-бит совпадает с
текущим классическим `control_step` (тест `test_env_identity_parity`); все компоненты obs в заявленных
диапазонах нормализации.

**Артефакты:** `envs/{observation,action,reward,rollout_env}.py`, `agent/normalization.py`;
тесты: identity-паритет, диапазоны нормализации, границы `action`.

### Этап 3 — Shield (защитный контур)

**Цель:** детерминированный контур безопасности между актором и PID, не обучается, всегда активен, с
fallback. См. §9.

**Состав работ:**
- Уровень 1 (поправки): clip `α` и весов каналов к допустимым границам.
- Уровень 2 (коэффициенты): hard bounds `Kp/Ki/Kd`, rate-limit между тактами, консистентность знаков и
  диапазонов, OOD-детектор (выход состояния/коэффициентов за обучающее распределение).
- Уровень 3 (runtime safety): скорость изменения коэффициента, чрезмерные тормоза, реверс <60 узлов, срыв
  по курсу.
- `Projection to Safe Set`; `Fallback` → чистый классический PID/автопилот при OOD/грубом нарушении.
- Хуки штрафов: активация уровня → счётчик для `L_shield`; скачки коэффициентов → для `L_smooth`.

**Критерий готовности:** чистая функция `(поправки, coeffs, runtime_state) → (safe coeffs, флаги)`;
юнит-тесты на каждое правило; проверено переключение на fallback.

**Артефакты:** `agent/shield.py`; `tests/test_shield.py`.

### Этап 4 — PIDNN Actor + Critic + PPO

**Цель:** обучаемый актор, генерирующий поправки и веса каналов; PPO с многокомпонентным loss; обучение с
domain randomization. См. §10, §11.

**Состав работ:**
- `agent/pidnn_actor.py`: shared encoder (GRU/LSTM или MLP над окном) → shared dense → головы
  Heading / Brake / Reverse; отдельный выход весов `w_lon/w_lat`; параметры распределения `(mean, log_std)`.
- `agent/critic.py`: `V(s)`.
- `agent/ppo.py`: rollout-буфер, GAE, clipped surrogate; `L = L_ppo + λs·L_smooth + λp·L_phys(=0) +
  λsh·L_shield`; AdamW; логирование по каждому терму, манифест запуска (seed/сценарии/гиперпараметры).
- `runtime/train.py`: цикл обучения, curriculum, чекпоинты; **Shield встроен в inference-путь актора**.
- Инициализация около identity (α≈1) — старт обучения ≈ классика.

**Критерий готовности:** сеть обучается (reward растёт, штрафные термы падают); на штатном сценарии
проходит гейты §2 не хуже классики; greedy-инференс детерминирован.

**Артефакты:** `agent/{pidnn_actor,critic,ppo}.py`, `runtime/train.py`;
тесты: формы forward, компоненты loss, детерминизм greedy, identity-инициализация.

### Этап 5 — Оценка, статистика, GUI

**Цель:** приёмка по гейтам ТЗ (не «сеть обучилась») + демонстрационный GUI сравнения с классикой.
См. §13, ТЗ 5.1.5.

**Состав работ:**
- `runtime/evaluate.py`: приёмочная батарея seeded-`Scenario` (штат + все отказы + погода); метрики §2
  против порогов `config/requirements.py`; статобработка, разбор аномалий/деградаций; проверка обобщения на
  hold-out; интерпретация вклада нейромодулей (сеть vs отключённая сеть = классика).
- `gui/`: визуализация траектории, XTE/heading/скорости, вклада каналов и Shield, side-by-side
  «классика ↔ нейро». Выбор стека GUI (matplotlib/веб-дашборд) — на этом этапе (§17 п.5).

**Критерий готовности:** прохождение приёмочной батареи (гейты ТЗ 5.1.2–5.1.3); отчёт со статистикой;
GUI демонстрирует отличительные особенности относительно классики.

**Артефакты:** `runtime/evaluate.py`, `gui/*`, отчёт/ноутбук с результатами и статистикой.

### Этап 6 — Deployment

**Цель:** детерминированная поставка на стенд Заказчика. См. §14, ТЗ 5.1.4.

**Состав работ:**
- Заморозка весов PIDNN (+ PINN при наличии); экспорт весов **вместе с константами нормализации** одним
  артефактом.
- `runtime/deploy.py`: greedy-инференс, без обучения, Shield активна, fallback доступен; транспорт —
  `ICSBackend` (ПИВ на стенд).
- Согласование финальной спецификации ПИВ (`ICSInputs/ICSOutputs`, частота, `*Valid`-флаги; §17 п.2).
- Упаковка в `.exe` (PyInstaller/аналог) с сетевым обменом по ПИВ.
- Оформление по ГОСТ 19.101-2024 (спецификация, описание программы, исходный/исполняемый код, руководство
  оператора, паспорт).

**Критерий готовности:** интеграционный прогон на стенде Заказчика по ПИВ; воспроизводимый детерминизм
(одинаковый вход → одинаковый выход).

**Артефакты:** `runtime/deploy.py`, `.exe`, комплект отчётной документации.

### Этап 7 *(опция, параллельно с 4–5)* — PINN / PD-PINN Observer

**Цель:** восстановление скрытых параметров динамики → выполнение диагностических требований ТЗ 5.1.3 и
обогащение состояния актора. См. §12.

**Состав работ:**
- `agent/observer.py`: история состояний → Temporal Encoder (GRU/LSTM) → Dense → Physics Head →
  `μ̂, ŵ_cross, brake_eff, nws_eff, τ̂`.
- Обучение отдельным System Identification (без PPO) на датасете из симулятора.
- Активация швов: заполнить зарезервированные слоты obs реальными оценками; включить `L_phys` (`λp > 0`) с
  residual-штрафом за нефизичность.

**Критерий готовности:** оценки μ/ветра/эффективности валидны на hold-out (сравнение с ground-truth
сценария); `L_phys` коррелирует с нефизичным поведением.

**Артефакты:** `agent/observer.py`, скрипт/датасет SI, обновлённый loss с активным `L_phys`.

---

## 16. Технологический стек и решения

- **Python 3.14**, PyTorch (уже в `.venv`), Gymnasium (добавить), NumPy/Pandas, termcolor, matplotlib.
- **RL:** собственный компактный PPO на PyTorch (обоснование — §11); SB3 как запасной.
- **Реалтайм:** цикл 20 Гц (`DT=0.05`); инференс малой сети укладывается в такт; асинхронная телеметрия
  X-Plane сохраняется.
- **Воспроизводимость:** сквозной seeding, сериализуемые `Scenario`, манифесты запусков, единые константы
  нормализации train↔deploy.

---

## 17. Открытые вопросы / риски

1. **Writable weather/μ DataRefs в X-Plane 12** — какими DREF-ами задавать сцепление, порывы, видимость;
   не всё, что есть в [`DataRefs.txt`](DataRefs.txt), пишется извне. Research-задача Этапа 1.
2. **Протокол ПИВ со стендом** — финальная спецификация полей `ICSInputs/ICSOutputs`, частота, порядок
   валидации (`*Valid`-флаги). Согласовать с Заказчиком до Этапа 6.
3. **Sim-to-bench gap** — динамика X-Plane ≠ модели стенда; отсюда важность domain randomization и
   робастности (обучаем в т.ч. на «ужасных» сценариях).
4. **Закрылки (Flap head)** — *решено: пока не вводим*. Остаётся зарезервированной 6-й головой в
   архитектуре (mermaid), добавляется позже; на старте работаем с 5 регуляторами.
5. **Стек GUI** и объём (ТЗ 5.1.5.3) — уточнить на Этапе 5.
6. **Срок 28.07.2026, УГТ 3** — приоритезировать Этапы 0–5 (демонстратор + сравнение с классикой);
   обсервер (Этап 7) — если позволяет время.

---

*Этап 0 выполнен (§4). Следующий шаг: Этап 1 — `SimInterface` + X-Plane/ICS бэкенды + генератор сценариев (§15).*
