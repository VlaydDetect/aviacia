"""Приёмочные пороги из ТЗ (раздел 5).

Единый источник истины для reward-шейпинга (envs/reward.py) и приёмочных гейтов
(runtime/evaluate.py). Ссылки на пункты ТЗ приведены рядом с каждым значением.
См. docs/ТЗ_Интеграл-КБО-МС_ИСМПУ_итог_ф.pdf.
"""

# --------------------------------------------------------------------------- #
# Базовые требования (5.1.1.2) — действуют во всех прогонах
# --------------------------------------------------------------------------- #

# 5.1.1.2 — на H=30 м продольная ось ВС совмещена с осью ВПП (± 5 м)
CENTERLINE_ALIGN_AT_30M_M = 5.0
CENTERLINE_ALIGN_HEIGHT_M = 30.0
"""Высота, на которой проверяется совмещение с осью. Отдельной константой: без неё порог ± 5 м
не с чем связать, и его легко применить не в той точке траектории."""
# 5.1.1.2 — приземление в пределах первой трети ВПП / ≤ 900 м от торца
TOUCHDOWN_FIRST_THIRD_M = 900.0

# --------------------------------------------------------------------------- #
# Заход и посадка (5.1.2) — угловые допуски
# --------------------------------------------------------------------------- #

# 5.1.2.1 — при штатном функционировании: отклонения по курсу не более ≈ 0,7°
COURSE_DEVIATION_MAX_DEG = 0.7
# 5.1.2.1 — отклонение по глиссаде не более 0,5° для ILS Cat
GLIDESLOPE_DEVIATION_MAX_DEG = 0.5
# 5.1.2.2 — при некорректной/неполной конфигурации шасси: глиссада не более ± 0,7°
GLIDESLOPE_GEAR_FAULT_MAX_DEG = 0.7
# 5.1.2.3 — при отказе стабилизатора: удержание траектории снижения в пределах ± 1° глиссады
GLIDESLOPE_STAB_FAULT_MAX_DEG = 1.0
"""Три разных допуска по глиссаде — по видам отказа, а не один «глиссадный». Требования ТЗ
ослабляются вместе с тяжестью отказа, и сведение их к одному числу либо завышает требование к
отказному режиму, либо занижает к штатному."""

# --------------------------------------------------------------------------- #
# Движение по ВПП и руление (5.1.3)
# --------------------------------------------------------------------------- #

# 5.1.3.1 — удержание осевой линии ВПП при пробеге
XTE_ROLLOUT_MAX_M = 3.0
# 5.1.3.1 — удержание осевой линии при рулении на прямых участках
XTE_TAXI_MAX_M = 1.0
# 5.1.3.2 — отказ NWS: отклонение от оси до полной остановки
XTE_NWS_FAIL_MAX_M = 5.0

# 5.1.2.4 / 5.1.3.3 — удержание курса при отказе тяги/реверса
HEADING_FAULT_MAX_DEG = 5.0
# 5.1.3.3 — держать курс до снижения скорости ниже 30 узлов
HEADING_HOLD_UNTIL_KTS = 30.0
