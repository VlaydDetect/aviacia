# Классический контур (Этап 0)
numpy>=2.0
pandas>=2.0
termcolor>=2.0

# Разработка / тесты
pytest>=8.0

# Нейросетевой контур (Этап 4+) — раскомментировать по мере надобности
# torch>=2.0
# gymnasium>=0.29
