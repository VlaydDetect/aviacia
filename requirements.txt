# Классический контур (Этап 0)
numpy>=2.0
pandas>=2.0
openpyxl>=3.1
termcolor>=2.0

# Разработка / тесты
pytest>=8.0

# Офлайн-обучение и SFT inference (или установить package extra ``.[sft]``)
# torch>=2.2
